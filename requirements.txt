# Pinned transitive dependencies to remediate Mend-reported vulnerabilities.
# These are pulled in via XBlock, which does not cap either package, so the
# fixed versions resolve cleanly.

# CVE-2026-41066 (GHSA-vfmq-68hx-4jfw): lxml XXE / local file read via default
# entity resolution. Fixed in 6.1.0.
lxml>=6.1.0

WebOb>=1.8.10
